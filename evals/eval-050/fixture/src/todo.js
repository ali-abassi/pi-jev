#!/usr/bin/env node
// TODO CLI. Store: TODO_FILE or ./todos.json (JSON array {id, text, done}).
// add <text...> -> prints id. list -> lines "ID [ ]|[[x] TEXT". done <id>.
console.error("not implemented");
process.exit(1);
