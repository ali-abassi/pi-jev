import os
def load(path):
    """Parse KEY=VALUE lines: skip blanks and # comments, strip matching
    single/double quotes, expand $OTHER from earlier entries. Returns dict."""
    raise NotImplementedError
