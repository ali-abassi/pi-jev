const ITEMS = Array.from({ length: 10 }, (_, i) => ({ id: i + 1 }));
// TODO: listItems(page, perPage) -> { items, total, pages }; page is 1-based.
export function listItems(page, perPage) {
  throw new Error("not implemented");
}
