// TODO: order(deps) where deps maps node -> array of nodes it needs first.
// Return an array with every need before its user. Throw on cycles.
export function order(deps) {
  throw new Error("not implemented");
}
